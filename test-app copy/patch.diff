diff --git a/arithmatic.py b/arithmatic.py
--- a/arithmatic.py
+++ b/arithmatic.py
@@ -11,7 +11,7 @@ def sum(a, b):
     Returns:
         int or float: The result of adding a and b.
     """
-    return a * b
+    return a + b
 
 
 def multiply(a, b):

diff --git a/main.py b/main.py
--- a/main.py
+++ b/main.py
@@ -31,10 +31,6 @@ async def get_cart_total(user_id: int):
     if user_id not in shopping_carts:
         raise HTTPException(status_code=404, detail="Cart not found")
     
-    total = 0
     cart = shopping_carts[user_id]
-    i = 0
-    while i < len(cart):
-        sum(total, items_db[cart[i]]["price"])
-        i += 1
+    total = sum(0, sum(items_db[item_id]["price"] for item_id in cart))
             
     return {"total": total}
